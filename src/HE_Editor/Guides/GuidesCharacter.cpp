#include "../EditorGuides.h"

// ── Guides: the character and what it does ───────────────────────────────────
// A player you can drive, a camera that follows, jumping, animation, input
// bindings, and the enemies that react to any of it.
//
// The worked example for this whole track — "An NPC that chases the player" —
// lives in EditorGuides.cpp and is installed immediately after these pages.
// Read it first; it is the shape, and it already covers the NavMesh, the
// Character Controller and the Nav Agent, so a page here should link to it
// ("guides-npc-chase", or an anchor inside it) rather than say it again.
//
// Fill this in with Guides::page(...) entries. Everything you need is in
// EditorGuides.h.

namespace HE::Ed::Guides
{

std::vector<Docs::Page> characterPages()
{
	return {};
}

} // namespace HE::Ed::Guides
